/* ------------------------------------------------------------------ *
 *  FAMILY MEDICINE — placeholder
 *
 *  Registered but not yet written. A rotation with an empty `sections`
 *  array is treated as unavailable: its tab renders disabled. Fill in
 *  the sections below using the shape in src/data/_template.js and the
 *  tab enables itself — no other file needs to change.
 * ------------------------------------------------------------------ */

const familyMedicine = {
  id: "family-medicine",
  name: "Family Medicine",
  short: "FM",
  sections: [],
};

export default familyMedicine;
